'use client';

import Button from '@/components/ui/Button';
import DonationIcon from './DonationIcon';

const Donation = () => {
  return (
    <Button>
      <DonationIcon />
    </Button>
  );
};

export default Donation;
